/*
 * Stub matrix.h.  finufft.cpp does `#include <matrix.h>` when
 * MX_HAS_INTERLEAVED_COMPLEX is defined; the real header just declares
 * the same mxArray / mxClassID / mwSize types that our mex.h shim already
 * provides, so this file intentionally has no contents.
 */

#ifndef NUMBL_MEX_SHIM_MATRIX_H
#define NUMBL_MEX_SHIM_MATRIX_H

#include "mex.h"

#endif
